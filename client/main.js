//Meteor.subscribe('posts');

/*
// Updates a page title dynamically
Tracker.autorun(function() {
   console.log("Page Title: " + Session.get('pageTitle') );
});

Template.layout.helpers({
   pageTitle: function() {
      return Session.set('pageTitle','HockeyMovers');
   }
}); //*/
